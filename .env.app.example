# App-specific credentials (store inside each project directory)
# This is just a Node-friendly example; apps may use Docker envs,
# shell exports, iOS build settings, or other config mechanisms.
APP_DATABASE_URL=
APP_REDIS_URL=
APP_STORAGE_KEY=
