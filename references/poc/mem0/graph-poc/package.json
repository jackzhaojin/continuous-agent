{
  "name": "mem0-graph-poc",
  "version": "0.1.0",
  "description": "Minimal mem0 graph-mode POC: save / get / graph-traverse with the V3.0 metadata schema",
  "type": "module",
  "scripts": {
    "poc": "tsx src/graph-poc.ts",
    "cleanup": "tsx src/graph-poc.ts --cleanup"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mem0ai": "^2.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@types/node": "^20.0.0"
  }
}
