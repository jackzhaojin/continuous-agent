# Agent SDK Skills POC - Test Results

Generated: 2026-01-24T16:39:15.024Z
Project Root: /Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc

## Summary

| # | Test | Result |
|---|------|--------|
| 1 | Experiment 1: Baseline (No Skills Config) | PASS |
| 2 | Experiment 2: settingSources Only (No Skill Tool) | PASS |
| 3 | Experiment 3: Skill Tool Only (No settingSources) | PASS |
| 4 | Experiment 4: Full Config - List Available Skills | PASS |
| 5 | Experiment 5: User Skills Only | PASS |
| 6 | Experiment 6: Project Skills Only | PASS |
| 7 | Experiment 7: Trigger Bundled Project Skill | INCOMPLETE* |
| 8 | Experiment 8: Trigger User Skill (conversation-logger) | INCOMPLETE* |
| 9 | Experiment 9: Wrong CWD (Different Directory) | PASS |
| 10 | Experiment 10: No CWD Specified | PASS |

> **\*Note on Tests 7 & 8**: These tests returned `error_max_turns` because this test suite used `maxTurns: 3`, which is insufficient for skill invocation. Skills require multiple turns (invoke skill → load content → execute tools). When re-tested with `maxTurns: 10+`, **both skills work correctly**. See `test-extended-skills.ts` for successful skill invocation tests.

## Detailed Results

### 1. Experiment 1: Baseline (No Skills Config)

**Description**: Query without settingSources or Skill in allowedTools - skills should NOT work

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "allowedTools": [
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
I can see from my tool configuration that there are currently **no skills available** in your setup.

The Skill tool is available for invoking skills (which users might reference as "slash commands" like `/commit`, `/review-pr`, etc.), but the "Available skills" section in the tool description is empty, meaning no skills have been configured or registered in your current environment.

If you'd like to use skills, you would need to configure them first. Skills are specialized capabilities that can be invoked to handle specific tasks like creating commits, reviewing pull requests, generating PDFs, etc.
```


---

### 2. Experiment 2: settingSources Only (No Skill Tool)

**Description**: Has settingSources but Skill NOT in allowedTools

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
Based on the available skills in the system, here are the skills you can use:

## Available Skills

1. **claude-mcp-builder**
   - Guide for creating high-quality MCP (Model Context Protocol) servers
   - Use for building MCP servers to integrate external APIs or services
   - Supports Python (FastMCP) and Node/TypeScript (MCP SDK)

2. **claude-pdf**
   - Comprehensive PDF manipulation toolkit
   - Extract text and tables, create PDFs, merge/split documents, handle forms
   - Use for programmatically processing, generating, or analyzing PDFs

3. **claude-skill-creator**
   - Guide for creating effective skills
   - Use when you want to create or update skills that extend Claude's capabilities
   - Helps with specialized knowledge, workflows, or tool integrations

4. **conversation-logger**
   - Log Claude Code conversation sessions with timestamps in structured markdown
   - Use when you say "log this session", "convo log", or want to create a record of prompts and responses

5. **harness-build**
   - Implementation skill for autonomous app builder
   - Implements code changes, runs tests, and verifies functionality
   - Use for building/implementing specific tasks with testing and evidence

6. **harness-research**
   - Research and planning skill for autonomous app builder
   - Creates detailed implementation plans by analyzing codebase and identifying patterns
   - Use when a build agent needs a research plan

7. **harness-spec**
   - Transform user prompts into structured specification artifacts
   - Generates constitution, requirements, architecture, and task breakdown (TASKS.json)
   - Use when bootstrapping a new application build

8. **harness-validate**
   - Independent validation skill for autonomous app builder
   - Verifies tasks meet acceptance criteria without writing code
   - Use for independent QA verification

9. **poc-test-skill**
   - Test skill for Agent SDK Skills POC
   - Use when you say "poc test", "test skill", "skills poc", or to demonstrate bundled skills

You can invoke any skill using the `Skill` tool with the skill name. For example: `/commit`, `/review-pr`, or by calling the tool directly.
```


---

### 3. Experiment 3: Skill Tool Only (No settingSources)

**Description**: Has Skill in allowedTools but NO settingSources

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
I can see from the Skill tool description that there are currently **no skills available** in your environment.

The tool shows "Available skills:" followed by an empty section, which means no skills have been configured or installed yet.

Skills are specialized capabilities that can be invoked using slash commands (like `/commit`, `/review-pr`, etc.) to help with specific tasks. When skills are available, they would be listed in the tool's description, but currently your setup doesn't have any configured.

If you'd like to use skills, you would need to:
1. Install or configure skills for your Claude Code environment
2. Once configured, they would appear in the available skills list and could be invoked using the Skill tool or by referencing them with `/skill-name`
```


---

### 4. Experiment 4: Full Config - List Available Skills

**Description**: Full configuration with settingSources and Skill tool

**Prompt**: `What skills are available? List them all with their descriptions.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
Based on the available skills in the system, here are all the skills currently available:

## Available Skills

1. **claude-mcp-builder**
   - Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external services through well-designed tools. Use when building MCP servers to integrate external APIs or services, whether in Python (FastMCP) or Node/TypeScript (MCP SDK).

2. **claude-pdf**
   - Comprehensive PDF manipulation toolkit for extracting text and tables, creating new PDFs, merging/splitting documents, and handling forms. When Claude needs to fill in a PDF form or programmatically process, generate, or analyze PDF documents at scale.

3. **claude-skill-creator**
   - Guide for creating effective skills. This skill should be used when users want to create a new skill (or update an existing skill) that extends Claude's capabilities with specialized knowledge, workflows, or tool integrations.

4. **conversation-logger**
   - Log Claude Code conversation sessions with timestamps in structured markdown. Use when the user says "log this session", "convo log", or wants to create a record of prompts and responses for a project.

5. **harness-build**
   - Implementation skill for the autonomous app builder. Implements code changes, runs tests, and verifies functionality. Use when you need to build/implement a specific task with testing and evidence.

6. **harness-research**
   - Research and planning skill for the autonomous app builder. Creates detailed implementation plans by analyzing the codebase, identifying patterns, and outlining step-by-step approaches. Use when a build agent needs a research plan for a specific task.

7. **harness-spec**
   - Transform user prompts into structured specification artifacts for autonomous app building. Use when bootstrapping a new application build from a comprehensive user prompt. Generates constitution, requirements (WHY/WHAT), architecture (HOW), and machine-readable task breakdown (TASKS.json).

8. **harness-validate**
   - Independent validation skill for the autonomous app builder. Verifies that tasks meet all acceptance criteria without writing code. Use when you need independent QA verification of a completed task.

9. **poc-test-skill**
   - A test skill for the Agent SDK Skills POC. Use when the user says "poc test", "test skill", "skills poc", or asks to demonstrate bundled skills.

---

These skills can be invoked using the Skill tool. For example, to use the PDF skill, you would invoke it with `skill: "claude-pdf"`, or to create a new skill, you would use `skill: "claude-skill-creator"`.
```


---

### 5. Experiment 5: User Skills Only

**Description**: settingSources with only "user" - should only find ~/.claude/skills/

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "user"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
Based on the available skills in the system, here are the skills you can use:

1. **claude-mcp-builder** - Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external services through well-designed tools. Use when building MCP servers to integrate external APIs or services, whether in Python (FastMCP) or Node/TypeScript (MCP SDK).

2. **claude-pdf** - Comprehensive PDF manipulation toolkit for extracting text and tables, creating new PDFs, merging/splitting documents, and handling forms. Use when you need to fill in a PDF form or programmatically process, generate, or analyze PDF documents at scale.

3. **claude-skill-creator** - Guide for creating effective skills. Use when you want to create a new skill (or update an existing skill) that extends Claude's capabilities with specialized knowledge, workflows, or tool integrations.

4. **conversation-logger** - Log Claude Code conversation sessions with timestamps in structured markdown. Use when you want to say "log this session", "convo log", or create a record of prompts and responses for a project.

5. **harness-build** - Implementation skill for the autonomous app builder. Implements code changes, runs tests, and verifies functionality. Use when you need to build/implement a specific task with testing and evidence.

6. **harness-research** - Research and planning skill for the autonomous app builder. Creates detailed implementation plans by analyzing the codebase, identifying patterns, and outlining step-by-step approaches. Use when a build agent needs a research plan for a specific task.

7. **harness-spec** - Transform user prompts into structured specification artifacts for autonomous app building. Use when bootstrapping a new application build from a comprehensive user prompt. Generates constitution, requirements (WHY/WHAT), architecture (HOW), and machine-readable task breakdown (TASKS.json).

8. **harness-validate** - Independent validation skill for the autonomous app builder. Verifies that tasks meet all acceptance criteria without writing code. Use when you need independent QA verification of a completed task.

You can invoke any of these skills using the Skill tool with the skill name (e.g., `/claude-pdf`, `/conversation-logger`, etc.).
```


---

### 6. Experiment 6: Project Skills Only

**Description**: settingSources with only "project" - should only find ./.claude/skills/

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
Based on the system configuration, there is currently **1 skill available**:

## Available Skills

### 1. **poc-test-skill**
- **Purpose**: A test skill for the Agent SDK Skills POC
- **When to use**: When you say:
  - "poc test"
  - "test skill" 
  - "skills poc"
  - Or ask to demonstrate bundled skills

---

**Note**: This appears to be a minimal skills setup, likely because this is primarily a development/experimentation environment for the Azure DA.live MCP monorepo. The skills system is extensible, and more skills can be added as needed for specific workflows (like commit helpers, PR review tools, etc.).
```


---

### 7. Experiment 7: Trigger Bundled Project Skill

**Description**: Attempt to trigger the poc-test-skill bundled in .claude/skills/

**Prompt**: `poc test - demonstrate the skills POC`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ⚠️ INCOMPLETE (hit maxTurns limit)

**Output**:
```
No output
```

**Error**: error_max_turns

**Root Cause**: Test used `maxTurns: 3` which is too low. Skill invocation requires:
- Turn 1: Claude calls Skill tool
- Turn 2: Skill content loaded, Claude processes instructions
- Turn 3+: Claude executes follow-up tools

**Verified Working**: Re-tested with `maxTurns: 10` in `debug-skill-invocation.ts` - **skill works correctly**. 


---

### 8. Experiment 8: Trigger User Skill (conversation-logger)

**Description**: Attempt to trigger the conversation-logger skill from ~/.claude/skills/

**Prompt**: `log this session`

**Config**:
```json
{
  "cwd": "/Users/jackjin/dev/azure-da-mcp/agent-claude-sdk/agent-sdk-skills-poc",
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash",
    "Write"
  ]
}
```

**Result**: ⚠️ INCOMPLETE (hit maxTurns limit)

**Output**:
```
No output
```

**Error**: error_max_turns

**Root Cause**: Test used `maxTurns: 3` which is too low. conversation-logger skill needs multiple turns to:
- Invoke Skill tool
- Check for existing prompt-log.md (Glob)
- Determine save location (Read directory)
- Create/write the file (Write)

**Verified Working**: Re-tested with `maxTurns: 15` in `test-extended-skills.ts` - **skill works correctly and creates files**. 


---

### 9. Experiment 9: Wrong CWD (Different Directory)

**Description**: Test with cwd pointing to a different directory - project skills should NOT load

**Prompt**: `What skills are available? Is poc-test-skill available?`

**Config**:
```json
{
  "cwd": "/tmp",
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
I can check the available skills for you by looking at the Skill tool's description.

Based on the Skill tool definition, here are the **currently available skills**:

1. **claude-mcp-builder** - Guide for creating MCP (Model Context Protocol) servers
2. **claude-pdf** - PDF manipulation toolkit for extracting text, creating PDFs, merging/splitting documents
3. **claude-skill-creator** - Guide for creating effective skills
4. **conversation-logger** - Log Claude Code conversation sessions with timestamps
5. **harness-build** - Implementation skill for autonomous app builder
6. **harness-research** - Research and planning skill for autonomous app builder
7. **harness-spec** - Transform user prompts into structured specification artifacts
8. **harness-validate** - Independent validation skill for autonomous app builder

**No, `poc-test-skill` is not currently available** in the list of skills. The available skills are limited to those listed above. 

If you need `poc-test-skill`, it would need to be created and registered first. You could use the **claude-skill-creator** skill to help create a new skill if needed.
```


---

### 10. Experiment 10: No CWD Specified

**Description**: Test without specifying cwd at all

**Prompt**: `What skills are available? List them.`

**Config**:
```json
{
  "settingSources": [
    "user",
    "project"
  ],
  "allowedTools": [
    "Skill",
    "Read",
    "Bash"
  ]
}
```

**Result**: ✅ SUCCESS

**Output**:
```
Based on the available tools, here are the skills currently available:

## Available Skills

1. **claude-mcp-builder**
   - Guide for creating high-quality MCP (Model Context Protocol) servers
   - Use when building MCP servers to integrate external APIs or services
   - Supports Python (FastMCP) and Node/TypeScript (MCP SDK)

2. **claude-pdf**
   - Comprehensive PDF manipulation toolkit
   - Extract text and tables, create new PDFs, merge/split documents, handle forms
   - Use for filling PDF forms or programmatically processing/generating/analyzing PDFs

3. **claude-skill-creator**
   - Guide for creating effective skills
   - Use when creating new skills or updating existing ones
   - Extends Claude's capabilities with specialized knowledge, workflows, or tool integrations

4. **conversation-logger**
   - Log Claude Code conversation sessions with timestamps in structured markdown
   - Use when you want to save session records ("log this session", "convo log")

5. **harness-build**
   - Implementation skill for autonomous app building
   - Implements code changes, runs tests, verifies functionality
   - Use for building/implementing specific tasks with testing and evidence

6. **harness-research**
   - Research and planning skill for autonomous app building
   - Creates detailed implementation plans by analyzing codebase
   - Use when a build agent needs a research plan for a task

7. **harness-spec**
   - Transform user prompts into structured specification artifacts
   - Use when bootstrapping a new application build
   - Generates constitution, requirements, architecture, and task breakdown (TASKS.json)

8. **harness-validate**
   - Independent validation skill for autonomous app building
   - Verifies tasks meet acceptance criteria without writing code
   - Use for independent QA verification of completed tasks

9. **poc-test-skill**
   - Test skill for the Agent SDK Skills POC
   - Use when demonstrating bundled skills ("poc test", "test skill", "skills poc")

You can invoke any of these skills using the `Skill` tool with the skill name. For example:
```
Skill(skill: "claude-pdf")
Skill(skill: "conversation-logger")
```
```


---

